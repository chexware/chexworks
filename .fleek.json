{
    "build": {
      "image": "node:lts",
      "command": "npm install -g pnpm && pnpm install && pnpm run build && echo $SOME_ENV",
      "publicDir": "build",
      "baseDir": "/",
      "environment": {
        "SOME_ENV": "Build finished!"
      }
    }
}